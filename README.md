# Email Spam Detector 📩

This is a simple Python-based email spam detection system.

## Features
- Uses TF-IDF vectorization
- Logistic Regression model
- CLI-based interface

## Run
pip install -r requirement.txt
python run.py
